# Conventions

_Not yet written._

Meant to capture the naming, tiering, and constraint rules referenced across the repo, e.g.:

- File naming patterns per folder (`companies/<TICKER>/tearsheet-<YYYY-MM-DD>.md`, `briefs/weekly/<YYYY-Www>.md`, `briefs/quarterly/<YYYY-Qn>.md`, `decisions/<YYYY-MM-DD>-<slug>.md`, `screens/<YYYY-Qn>-<slug>.md`)
- Watchlist tiers / status values
- Any hard constraints on the process (data sources allowed, rubric version in force, etc.)

TODO: fill in from prior project-chat discussion.
