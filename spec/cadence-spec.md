# Cadence Spec

_Not yet written._

This is meant to be the contract for the repo's recurring cadences (weekly watchlist brief, quarterly screen/review, etc.) — what runs, on what schedule, what inputs it reads, and what output it's expected to produce under `briefs/`, `screens/`, and `companies/`.

TODO: define each cadence (trigger, inputs, output location/naming, owner skill under `skills/`).
